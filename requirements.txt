pennylane>=0.40
pyscf>=2.5
numpy>=1.26
scipy>=1.11
matplotlib>=3.8

# `make test` is a documented step, so the runner belongs in the one
# environment `make setup` builds. Nothing above pulls it in transitively.
pytest>=8.0
